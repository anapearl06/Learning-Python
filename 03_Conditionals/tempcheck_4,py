device_status = "active"
temperature = 38

if device_status == "active":
    if temperature > 35:

        print("High Temperature Alert")
    else:

        print("Temperature is Normal")

        #else statement here is opotional
        #we can write number of nested if else conditions here 

else: 
    print("Device is offline")
