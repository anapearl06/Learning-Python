def add_vat(price, vat_rate):
    """Calculate the price including VAT."""
    return price * (100 + vat_rate)/100


orders = [100, 200, 300]

for order in orders:
    price_with_vat = add_vat(order, 20)
    print(f"Price including VAT: {price_with_vat}")     
    